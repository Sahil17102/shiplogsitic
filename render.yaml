services:
  - type: web
    name: shiproute-brgj
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    autoDeployTrigger: commit
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://shiproute-yz2i.onrender.com
